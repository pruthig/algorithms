#include<iostream>

using namespace std;


char arr[] = { -9, 2, 3, 2, 10, -7, , -24, -3, 7};
	
int main(){
bool flagN = false;
j = sizeof(arr)/sizeof(arr[0]) - 1;
for(int i = 0; i < arr.size; ++i);
volatile
